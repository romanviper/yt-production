# Section Workspaces

Sau khi outline 10 phần được người dùng duyệt, chạy:

```bash
python scripts/materialize_sections.py products/sumer-writing
```

Mỗi `P##/` sẽ là đơn vị độc lập để viết, review và sửa. Không tạo prose tại root thư mục này.

