# Task Report — T0016 design_section P01

## Result

Story plan P01 đã được sửa theo toàn bộ `story-plan-change-request.md`. Artifact vẫn ở trạng thái `draft`; không có narration/prose và không tự phê duyệt.

## Fields changed from the task baseline

- `status`: `not_started` → `draft`.
- `governing_idea`: đặt giới hạn duy nhất ở khả năng các dấu cố định một phần thông tin trên vật thể bền dù chưa ghi trọn lời nói.
- `audience_question`: chuyển trọng tâm từ “dịch câu gì” sang “các dấu đã làm được gì”.
- `audience_payoff`: dùng đúng cùng giới hạn khẳng định với `governing_idea`.
- `evidence_roles`: phân vai đủ chín claim trong packet; giữ nguyên phạm vi evidence và guardrails của vòng thiết kế trước.
- `claim_use`: loại bỏ suy diễn về cuộc trao đổi/người tham gia; gộp phần định nghĩa thành một explanatory turn phổ thông; hạ chronology và administrative context xuống vai trò hỗ trợ.
- `beats`: thay bằng đúng năm beat `hook → tension → reveal → payoff → bridge`; chronology không sở hữu beat.
- `terminology`: chỉ giữ `proto-cuneiform`, kèm giải thích phổ thông.
- `opening_move`: mở bằng giới hạn đọc của P005390, không dựng scene hay catalogue niên đại.
- `ending_move`: trả payoff giới hạn rồi mở câu hỏi về quá trình hình thành.
- `comprehension_test`: kiểm tra đúng khẳng định giới hạn của governing idea/payoff.

## Evidence allocation

- Narrated: `CLM-0042`, `CLM-0001`, `CLM-0009`.
- Support: `CLM-0002`, `CLM-0004`.
- Guardrail: `CLM-0006`, `CLM-0007`, `CLM-0008`.
- Omit: `CLM-0034` (trùng nội dung với `CLM-0004`).

## Change-request compliance

1. Không nhắc đến cuộc trao đổi hoặc người tham gia.
2. Hai beat định nghĩa cũ được gộp thành một explanatory turn ngắn ở beat reveal.
3. Chronology chỉ là định hướng tối thiểu trong bridge/claim use, không có beat riêng.
4. Có đúng năm beat theo thứ tự yêu cầu.
5. Thuật ngữ duy nhất là `proto-cuneiform`.
6. `governing_idea` và `audience_payoff` chỉ khẳng định khả năng cố định một phần thông tin trên vật thể bền dù chưa ghi trọn lời nói.

## Validation

- Product validation: passed.
- Task packet verification: passed.
- Scope check: passed.
- Operator brief validation: passed.

